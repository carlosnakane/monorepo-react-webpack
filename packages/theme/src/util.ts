

const createShortHandDimensions = (dimensions: { top: string, right: string, bottom: string, left: string }) =>
 `${dimensions.top} ${dimensions.right} ${dimensions.bottom} ${dimensions.left}`;

export { createShortHandDimensions };
